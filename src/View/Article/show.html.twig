{% extends 'layout.html.twig' %}

{% block content %}
    <h1>Article {{ article.title }}</h1>
    <p>{{ article.id }}. {{ article.title }}</p>
    
    <a href="/article/index">Back</a>
{% endblock %}
